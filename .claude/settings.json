{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json,subprocess; d=json.load(sys.stdin); f=d.get('tool_input',{}).get('file_path',''); (f.endswith('.py') and subprocess.run(['black', f])) or (any(f.endswith(x) for x in ['.js','.css']) and subprocess.run(['prettier','--write',f]))\""
          }
        ]
      }
    ]
  }
}